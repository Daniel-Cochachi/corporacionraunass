---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Navbar from '../../components/Navbar.astro';
import Footer from '../../components/Footer.astro';
import WhatsAppButton from '../../components/WhatsAppButton.astro';
import Icon from '../../components/Icon.astro';
import CTA from '../../components/CTA.astro';

const items = [
    "Plantas Chancadoras (móviles/estáticas) y Dosificadores.",
    "Plantas de asfalto.",
    "Fajas Transportadoras.",
    "Zarandas Vibratorias.",
    "Tolvas Alimentadoras.",
    "Tornillo Sin Fin."
];
---

<BaseLayout title="Minería - Corporación Raunass">
    <Navbar />
    <main>
        <section class="service-detail">
            <div class="container">
                <div class="detail-header">
                    <span class="category">Servicios</span>
                    <h1>Minería</h1>
                    <p class="lead">Especialistas en la fabricación de fajas transportadoras y plantas de procesamiento para la gran y mediana minería.</p>
                </div>

                <div class="content-grid">
                    <div class="image-box">
                        <img src="/servicio-mineria.webp" alt="Servicios de Minería" />
                    </div>
                    <div class="info-box">
                        <p>Brindamos soluciones robustas diseñadas para resistir las condiciones más extremas del sector minero. Nuestros equipos optimizan el transporte y la clasificación de materiales, integrando tecnología de punta y materiales de alta resistencia.</p>
                        
                        <ul class="detail-list">
                            {items.map(item => (
                                <li>
                                    <Icon name="check" size={20} color="var(--accent)" />
                                    <span>{item}</span>
                                </li>
                            ))}
                        </ul>
                    </div>
                </div>
            </div>
        </section>
        <CTA />
    </main>
    <Footer />
    <WhatsAppButton />
</BaseLayout>

<style>
    .service-detail {
        padding: 5rem 0;
    }

    .detail-header {
        margin-bottom: 4rem;
        max-width: 800px;
    }

    .category {
        color: var(--accent);
        font-weight: 700;
        text-transform: uppercase;
        font-size: 0.9rem;
        letter-spacing: 1px;
        display: block;
        margin-bottom: 1rem;
    }

    .detail-header h1 {
        font-size: 3.5rem;
        margin-bottom: 1.5rem;
    }

    .lead {
        font-size: 1.25rem;
        color: var(--text-light);
    }

    .content-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 4rem;
        align-items: start;
    }

    .image-box img {
        width: 100%;
        border-radius: var(--radius);
        box-shadow: var(--shadow);
    }

    .info-box p {
        font-size: 1.1rem;
        line-height: 1.8;
        margin-bottom: 2.5rem;
    }

    .detail-list {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .detail-list li {
        display: flex;
        align-items: center;
        gap: 1rem;
        padding: 1rem;
        background: var(--bg-alt);
        border-radius: 8px;
        transition: var(--transition);
    }

    .detail-list li:hover {
        transform: translateX(10px);
        background: var(--white);
        box-shadow: var(--shadow-sm);
    }

    @media (max-width: 992px) {
        .content-grid {
            grid-template-columns: 1fr;
            gap: 2rem;
        }

        .detail-header h1 {
            font-size: 2.5rem;
        }
    }
</style>
